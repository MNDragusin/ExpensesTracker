@using ExpensesTracker.Pages
@inherits ExpensesBase
@page "/expenses"

<PageTitle>Expenses</PageTitle>
<h3>Expenses</h3>

<AuthorizeView>
    <Authorized>
        <div class="row">
            <div class="form-group col-5">
                <MudButton Href="AddNewExpense" Variant="Variant.Text" Color="Color.Primary">Add new</MudButton>
            </div>
        </div>

            <MudSimpleTable Hover="true" Dense="true" FixedHeader="true" Style="height:550px">
                <thead>
                <tr>
                    <th>Date</th>
                    <th>Wallet</th>
                    <th>Category</th>
                    <th>Label</th>
                    <th>Amount</th>
                    <th></th>
                </tr>
                </thead>
                <tbody>
                @foreach (var entry in _expenses)
                {
                    <tr>
                        <th>@entry.Date</th>
                        <th>@_wallets.FirstOrDefault(w => w.Id == entry.WalletId)!.Name</th>
                        <th>@_categories.FirstOrDefault(c => c.Id == entry.CategoryId)!.Name</th>
                        <th>@_labels.FirstOrDefault(l => l.Id == entry.LabelId)!.Name</th>
                        <th>@entry.Amount</th>
                        <th>
                            <div class="row">
                                <div class="col">
                                    <MudIconButton HRef="@($"/EditEntry/{entry.EntryId}")" Icon="@Icons.Material.Filled.Edit" Variant="Variant.Filled" Color="Color.Primary" Size="Size.Large" />
                                </div>
                                <div class="col">
                                    <form method="post" @onsubmit="@(()=>RemoveEntry(entry.EntryId))" @formname="@($"DeleteEntrt_{entry.EntryId}")">
                                    <AntiforgeryToken />
                                        <MudIconButton ButtonType="ButtonType.Submit" Icon="@Icons.Material.Filled.Delete" Variant="Variant.Filled" Color="Color.Secondary" Size="Size.Large" />
                                    </form>
                                </div>
                            </div>    
                        </th>
                    </tr>
                }
                </tbody>
            </MudSimpleTable>
    </Authorized>
    <NotAuthorized>
        <RedirectToLogin />
    </NotAuthorized>
</AuthorizeView>
