<?xml version="1.0" encoding="utf-8" ?>
<Border xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
        xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
        x:Class="MauiClient.Pages.Controls.TagView"
        StrokeShape="RoundRectangle 16" 
        HeightRequest="32" 
        StrokeThickness="0" 
        Padding="{OnPlatform '12,0,12,8',Android='12,0,12,0'}">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="30"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        <Border  x:Name="ColorBadge" Grid.Column="0"/>
        <Label x:Name="TitleLabel" Grid.Column="1"
                TextColor="{AppThemeBinding Light={StaticResource LightBackground},Dark={StaticResource DarkBackground}}" 
                FontSize="14" 
                VerticalOptions="Center"
                VerticalTextAlignment="Center"/>    
    </Grid>

</Border>